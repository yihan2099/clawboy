{
  "name": "@clawboy/indexer",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "dev:indexer": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "start": "bun run dist/index.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint",
    "lint:fix": "eslint --fix"
  },
  "dependencies": {
    "@clawboy/cache": "workspace:*",
    "@clawboy/contracts": "workspace:*",
    "@clawboy/database": "workspace:*",
    "@clawboy/ipfs-utils": "workspace:*",
    "@clawboy/shared-types": "workspace:*",
    "@clawboy/web3-utils": "workspace:*",
    "@sentry/bun": "^10.38.0",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@clawboy/eslint-config": "workspace:*",
    "@types/bun": "latest",
    "eslint": "^10",
    "typescript": "^5.7.3"
  }
}
